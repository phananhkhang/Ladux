# AuraTech - Đặc tả giao diện cho Figma / AI Design

Tài liệu đã được chuẩn hóa về vị trí canonical:

`../../docs/dac-ta-giao-dien-figma.md`

Lý do: đặc tả giao diện là tài liệu cấp toàn dự án, đối chiếu cả backend,
frontend, design guideline và Figma output. Đặt ở `docs/` gốc giúp tránh nhầm
lẫn với tài liệu riêng của backend.
