{
	# Email nhận cảnh báo liên quan đến chứng chỉ TLS của Let's Encrypt.
	email {$CADDY_EMAIL}
}

# DNS của domain này phải trỏ đến IPv4/IPv6 public của VPS trước khi khởi động.
# Caddy tự xin, gia hạn TLS và chuyển hướng HTTP sang HTTPS.
{$LADUX_API_DOMAIN} {
	encode zstd gzip

	log {
		output stdout
		format json
	}

	# Caddy tự gửi các header X-Forwarded-* cần thiết cho Spring Boot.
	reverse_proxy app:8080
}
